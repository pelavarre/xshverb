#!/bin/bash

# usage: pwnme
# update this Git Clone

D=$(dirname "$0")/..
cd "$D"/ || exit 1

echo >&2
echo + cd "$(dirs -p |head -1)/" >&2

set -xe

:
git log --oneline --no-decorate -3

:
git fetch --prune --prune-tags --force
git rebase

:
git log --oneline --no-decorate -3
